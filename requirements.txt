# Core runtime dependencies.
#
# This is the minimum needed to run the whole platform locally: data
# validation, training, tuning, the registry, the API, drift detection,
# retraining, and the LLMOps layer with the mock provider.
#
# Optional backends live in pyproject.toml extras and are NOT listed here:
#   [aws]      boto3, sagemaker           -- S3, SageMaker, CloudWatch, Bedrock
#   [quality]  great-expectations, evidently
#   [data]     dvc, dvc-s3
#   [boosting] xgboost, lightgbm
#   [llm]      anthropic, google-genai, openai
#   [dev]      pytest, ruff, black, mypy, bandit, pip-audit
#
# Install everything:  pip install -e ".[dev,aws,quality,data,boosting,llm]"

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0
PyYAML>=6.0

# --- data and modelling ---
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3

# --- experiment tracking and registry ---
mlflow>=2.16

# --- observability ---
prometheus-client>=0.20
psutil>=5.9

# --- http ---
httpx>=0.27
requests>=2.32
